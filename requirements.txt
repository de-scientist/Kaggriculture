# Runtime dependencies for the Kaggriculture submission (main.py + agent/).
# kaggle-environments is provided by the competition runtime; numpy is only
# needed for offline training (agent/learning/trainer.py) and is harmless here.
pyyaml>=6.0
numpy>=1.24
